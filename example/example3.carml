def fact n : int => int = if (< n 0) then
        (fact (abs n))
    else if (num= n 0) then
        1
    else
        (mul n (fact (sub n 1)))

def foo x : array[int] => int = (reduce fn y : int z : int => int = (sum y z) 0 x)
